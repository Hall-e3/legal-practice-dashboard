export enum ButtonType {
  button = "button",
  submit = "submit",
  reset = "reset",
}

export enum InputType {
  button = "button",
  checkbox = "checkbox",
  date = "date",
  email = "email",
  file = "file",
  radio = "radio",
  text = "text",
  tel = "tel",
  password = "password",
  number = "number",
}

export enum UserRole {
  admin = "admin",
  normal = "standard",
}

export enum Screens {
  login = "login",
  dashboard = "dashboard",
  home = "home",
}

export enum ResponseStatus {
  success = "success",
  error = "error",
  info = "info",
}
